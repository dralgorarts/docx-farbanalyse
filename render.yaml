services:
  - type: web
    name: docx-farbanalyse
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: API_KEY
        generateValue: true
    healthCheckPath: /health
